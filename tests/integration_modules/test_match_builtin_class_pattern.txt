$ desugars match_builtin_class_pattern

"""Replicates CPython's dataclasses slot handling using a str class pattern."""

match "aa":
    case str(slot):
        MATCHED = slot
    case _:
        MATCHED = None
=
"""Replicates CPython's dataclasses slot handling using a str class pattern."""
import __dp__
_dp_match_1 = "aa"
_dp_tmp_2 = isinstance(_dp_match_1, str)
if _dp_tmp_2:
    _dp_tmp_2 = __dp__.match_class_attr_exists(str, _dp_match_1, 0, 1)
if _dp_tmp_2:
    slot = __dp__.match_class_attr_value(str, _dp_match_1, 0, 1)
    MATCHED = slot
else:
    MATCHED = None
